"""
lists use square brackets and tuples use rounded
"""
empty_tuple = ()
one_el_tuple_a = (1,)
one_el_tuple_b = 1,
three_el_tuple = 1, 2, 3,
print(three_el_tuple

#mutible data can be updated anytime. like in a list
# tuples stay this way forever. they are NOT mutable
#strings are also imutable like tuples